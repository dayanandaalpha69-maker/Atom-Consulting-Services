name: Streamlit keepalive

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Request public app
        run: curl --fail --silent --show-error --max-time 30 https://atomconsultingservices.streamlit.app/ > /dev/null